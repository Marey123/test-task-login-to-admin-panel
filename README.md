# Run server

In the project directory, you can run:

### `cd backend`
### `yarn`
create env file (use env.example)
### `node server.js`

# Run react app

In the project directory, you can run:

### `yarn`
### `yarn start`
